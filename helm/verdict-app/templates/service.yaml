apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceAccount.name | default "verdict-app" }}
  namespace: verdict
  labels:
    app: verdict-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: verdict-app
